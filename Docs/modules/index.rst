Python modules
==============

This section documents each Python module at the repository root: what it is
for, how it fits into the call graph described in :doc:`../introduction`, and
where it implements the equations from :doc:`../model`. Each page combines a
prose description, the key function signatures reproduced verbatim via
``literalinclude`` (for the functions that directly implement a manuscript
equation), and a full auto-generated API reference for the rest of the
module.

.. toctree::
   :maxdepth: 1

   constants
   reactions
   field_distributions
   inception
   eigenvalues
   ionization_integral
   lambda_growth_rate
   create_chombo_discharge_data
